import type{BeltRank,Graduation,StudentHistoryEvent}from'../types';import{httpService}from'./http.service';export const historyService={history:(id:number)=>httpService.get<StudentHistoryEvent[]>(`/students/${id}/history`),graduations:(id:number)=>httpService.get<Graduation[]>(`/students/${id}/graduations`),createGraduation:(id:number,body:{modalityId:number;belt:BeltRank;beltStartedAt:string;graduatedAt:string;notes?:string})=>httpService.post<Graduation,typeof body>(`/students/${id}/graduations`,body)};
